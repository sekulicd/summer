kind: pipeline
type: docker
name: default

steps:
  - name: ci
    image: golang
    volumes:
      - name: deps
        path: /go
    commands:
      - make ci
      - ls -lrt
  - name: cd
    image: golang
    volumes:
      - name: deps
        path: /go
    commands:
      - docker --version
      - ls -lrt
      - make cd

volumes:
  - name: deps
    temp: {}
