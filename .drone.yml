kind: pipeline
name: default

steps:
  - name: ci
    image: golang
    volumes:
      - name: deps
        path: /go
    commands:
      - make ci
      - ls -lrt
  - name: cd
    image: golang
    volumes:
      - name: deps
        path: /go
    commands:
      - make cd
      - ls -lrt

volumes:
  - name: deps
    temp: {}
