kind: pipeline
type: docker
name: default

steps:
  - name: ci
    image: golang
    volumes:
      - name: deps
        path: /go
    commands:
      - make ci
      - ls -lrt
  - name: cd
    image: docker
    volumes:
      - name: deps
        path: /go
    commands:
      - make cd

volumes:
  - name: deps
    temp: {}
